# React Vite テンプレート

React + Vite + React Router を使った学習用プロジェクト

## 📚 ブランチ別学習内容

### `master`

- 基本的な React + Vite のセットアップ

### `feature-cat` / `feature-tiger`

- SVG アセットの読み込みと使用
- 環境変数（`import.meta.env.DEV`）の活用

### `feature/transition`

- React Router の基本的なルーティング
- ページ遷移の実装

### `feature/actions`

- コンポーネント間のアクション管理

### `feature/histories` / `develop/histories` / `develop02/histories`

- **React Router のレイアウト構造**
- **MainLayout と PageLayout の使い分け**
- **Outlet と children の違い**
- **訪問履歴（History）の実装**
- **2重ラップのアンチパターン**

## 🚀 起動方法

```bash
npm install
npm run dev
```

## 🔧 開発コマンド

```bash
npm run dev      # 開発サーバー起動
npm run build    # ビルド
npm run preview  # ビルド結果をプレビュー
npm run format   # コードフォーマット
npm run lint     # リント実行
```
